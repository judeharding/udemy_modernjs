var num = 6;

var mod = num % 2;

console.log(mod);

// ---

var userName;

userName = null;

if ((userName !== undefined) && (userName !== null)){
    console.log("User is: " + userName);
} else {
    console.log ("User is not defined");
}
